# Copy to .env and adjust if the backend is running on a different URL.
# When using the Vite dev server the /api prefix is proxied to the backend.
# For production builds set this to the full backend URL:
# VITE_API_URL=http://localhost:8000
