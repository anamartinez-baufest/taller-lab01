version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: jwt-backend
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=changeme-supersecret-key-change-in-production
    restart: unless-stopped
